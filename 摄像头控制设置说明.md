# 摄像头控制设置说明

## 新增功能

在 `AEWPlayerController` 类中添加了两个新的摄像头控制设置：

### 1. 鼠标Y轴反转控制 (bInvertMouseY)
- **类型**: `bool`
- **默认值**: `false`
- **用途**: 控制鼠标上下移动与摄像头上下移动的关系
- **设置位置**: 编辑器中的 "Camera Settings" 分类
- **蓝图访问**: 可以在蓝图中读写

**行为说明**:
- `false` (默认): 鼠标向上移动时，摄像头向上看
- `true`: 鼠标向上移动时，摄像头向下看 (飞行模拟器风格)

### 2. 鼠标灵敏度控制 (MouseSensitivity)
- **类型**: `float`
- **默认值**: `1.0f`
- **取值范围**: 0.1 - 10.0
- **用途**: 控制鼠标移动的敏感程度
- **设置位置**: 编辑器中的 "Camera Settings" 分类
- **蓝图访问**: 可以在蓝图中读写

**行为说明**:
- `0.5`: 降低灵敏度，需要更大的鼠标移动才能转动相同角度
- `1.0`: 标准灵敏度
- `2.0`: 提高灵敏度，小幅鼠标移动就能转动较大角度

## 应用范围

这些设置会应用到以下情况：

1. **普通视角控制**: 当没有锁定目标时的自由视角
2. **锁定模式视角**: 当锁定目标时的相机控制 (会自动降低50%灵敏度)

## 在编辑器中设置

1. 选择你的 PlayerController 蓝图或实例
2. 在 Details 面板中找到 "Camera Settings" 分类
3. 调整以下设置：
   - **Invert Mouse Y**: 勾选以启用Y轴反转
   - **Mouse Sensitivity**: 调整滑块设置灵敏度 (范围 0.1-10.0)

## 在蓝图中动态修改

你可以在游戏运行时通过蓝图动态修改这些设置：

### 设置Y轴反转
```
Get Player Controller -> Cast to EWPlayerController -> Set Invert Mouse Y
```

### 设置鼠标灵敏度
```
Get Player Controller -> Cast to EWPlayerController -> Set Mouse Sensitivity
```

### 获取当前设置
```
Get Player Controller -> Cast to EWPlayerController -> Get Invert Mouse Y
Get Player Controller -> Cast to EWPlayerController -> Get Mouse Sensitivity
```

## 实现细节

- 设置会实时应用，无需重启游戏
- 锁定模式下会自动应用额外的灵敏度减半
- Y轴反转同时作用于普通模式和锁定模式
- 灵敏度设置会与原始输入值相乘

## 建议的默认设置

- **鼠标Y轴反转**: `false` (适合大多数玩家)
- **鼠标灵敏度**: `1.0` (可根据玩家偏好调整)

## 未来扩展建议

1. 添加X轴和Y轴独立的灵敏度控制
2. 添加锁定模式专用的灵敏度设置
3. 支持保存到用户设置文件
4. 添加平滑/加速曲线选项
